[phases.setup]
# Install Python + pip
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip",
  "pip install -r requirements-railway.txt"
]

[start]
cmd = "python real_main.py"
